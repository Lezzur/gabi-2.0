/**
 * GAIA Border Radius Tokens — pixel numbers for React Native compatibility.
 *
 *   sm   →  input fields, alert banners
 *   md   →  buttons, badges
 *   lg   →  cards
 *   xl   →  modals, bottom sheets
 *   full →  pill badges, chips, tags
 */

export const radii = {
  sm:   4,
  md:   8,
  lg:   12,
  xl:   16,
  full: 9999,
} as const;
